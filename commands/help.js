const { execute } = require("./remove_back");

module.exports = {
    name: 'help',
    aliases: [],
    description: 'this command prints all of this bot\'s commands',
    async execute(client, message, args) {

    }
}